BASE="${HOME}/tools"

mkdir -p "${BASE}"
cd "${BASE}"

# Change to the git repos
cd "${BASE}"

# clone it to the folder
git clone https://github.com/fischuu/eggnog7_annotator.git

# Export it to make the command system-wide available
export PATH="${BASE}/eggnog7_annotator:$PATH"
